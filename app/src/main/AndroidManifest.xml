<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.avayacountycouncil">

    <uses-feature
        android:name="android.hardware.Camera"
        android:required="True" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".Paymentpage2"></activity>
        <activity android:name=".CommentsLocation" />
        <activity android:name=".ReportPotholeV2" />
        <activity android:name=".AIBotActivity1" />
        <activity android:name=".PayPalActivity" />
        <activity android:name=".YoutubeActivity2" />
        <activity android:name=".Main2DashboardActivity" />
        <activity android:name=".ReportPotholeActivity" />
        <activity android:name=".Menu1Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>